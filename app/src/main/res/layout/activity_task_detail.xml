<?xml version="1.0" encoding="utf-8"?>
<!--
    The root is a ScrollView to ensure all content is accessible on smaller screens,
    as the form can be quite long.
    - 'fillViewport="true"' allows the child ConstraintLayout to fill the screen height.
-->
<ScrollView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:background="#F5F5F5"
    android:fillViewport="true">

    <!-- A ConstraintLayout is used inside the ScrollView for flexible positioning of all form elements. -->
    <androidx.constraintlayout.widget.ConstraintLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        tools:context=".TaskDetailActivity">

        <!-- The main header text for the screen ("Define a new task" or "Edit Your Task"). -->
        <TextView
            android:id="@+id/screenHeader"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="32dp"
            android:fontFamily="@font/zalando_sans_semi_expanded_black"
            android:text="Define a new task to execute"
            android:textColor="@android:color/black"
            android:textSize="20sp"
            android:textStyle="bold"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toTopOf="parent" />

        <!--
            This CardView creates the "sticky note" visual for the title and description.
        -->
        <androidx.cardview.widget.CardView
            android:id="@+id/stickyNoteCard"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="16dp"
            android:layout_marginTop="16dp"
            android:layout_marginEnd="16dp"
            app:cardCornerRadius="12dp"
            app:cardElevation="8dp"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@+id/screenHeader">

            <!-- A LinearLayout to stack the title and description vertically. -->
            <LinearLayout
                android:layout_width="match_parent"
                android:layout_height="wrap_content"
                android:orientation="vertical">

                <!-- EditText for the task title. -->
                <EditText
                    android:id="@+id/taskTitleEditText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@color/default_note_dark"
                    android:fontFamily="@font/zalando_sans_semi_expanded_black"
                    android:hint="Title"
                    android:inputType="textCapWords"
                    android:padding="16dp"
                    android:textColor="@android:color/black"
                    android:textStyle="bold" />

                <!-- EditText for the task description. 'minHeight' ensures it's a decent size. -->
                <EditText
                    android:id="@+id/taskDescriptionEditText"
                    android:layout_width="match_parent"
                    android:layout_height="wrap_content"
                    android:background="@null"
                    android:gravity="top"
                    android:hint="Describe the Task here"
                    android:inputType="textMultiLine"
                    android:minHeight="120dp"
                    android:padding="16dp"
                    android:textColor="@android:color/black" />
            </LinearLayout>
        </androidx.cardview.widget.CardView>

        <!--
            ImageView to display the task's associated image.
            - It's hidden by default ('visibility="gone"') and shown programmatically.
        -->
        <ImageView
            android:id="@+id/taskImageView"
            android:layout_width="0dp"
            android:layout_height="200dp"
            android:layout_marginTop="16dp"
            android:background="@drawable/search_bar_background"
            android:scaleType="centerCrop"
            android:visibility="gone"
            app:layout_constraintEnd_toEndOf="@id/stickyNoteCard"
            app:layout_constraintStart_toStartOf="@id/stickyNoteCard"
            app:layout_constraintTop_toBottomOf="@id/stickyNoteCard"
            tools:src="@tools:sample/backgrounds/scenic"
            tools:visibility="visible" />

        <!-- Button to allow the user to add or change an image. -->
        <Button
            android:id="@+id/addImageButton"
            style="@style/Widget.MaterialComponents.Button.OutlinedButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:text="Add Image"
            app:layout_constraintEnd_toEndOf="@id/stickyNoteCard"
            app:layout_constraintStart_toStartOf="@id/stickyNoteCard"
            app:layout_constraintTop_toBottomOf="@id/taskImageView" />

        <!-- A simple label for the color selector section. -->
        <TextView
            android:id="@+id/colorSelectorLabel"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Select Color"
            android:textStyle="bold"
            app:layout_constraintStart_toStartOf="@+id/stickyNoteCard"
            app:layout_constraintTop_toBottomOf="@id/addImageButton" />

        <!--
            A HorizontalScrollView allows the color buttons to be scrolled horizontally
            if they don't all fit on the screen. 'scrollbars="none"' hides the scrollbar.
        -->
        <HorizontalScrollView
            android:id="@+id/colorSelectorScrollView"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="8dp"
            android:scrollbars="none"
            app:layout_constraintEnd_toEndOf="@id/stickyNoteCard"
            app:layout_constraintStart_toStartOf="@id/stickyNoteCard"
            app:layout_constraintTop_toBottomOf="@id/colorSelectorLabel">

            <!-- A LinearLayout to hold all the color selector ImageButtons. -->
            <LinearLayout
                android:id="@+id/colorSelectorLayout"
                android:layout_width="wrap_content"
                android:layout_height="wrap_content"
                android:orientation="horizontal"
                android:paddingVertical="8dp">

                <!-- Each ImageButton represents a color choice. -->
                <ImageButton
                    android:id="@+id/colorDefault"
                    android:layout_width="48dp"
                    android:layout_height="48dp"
                    android:layout_marginHorizontal="8dp"
                    android:background="@drawable/color_selector_default"
                    android:contentDescription="Default Color" />
                <!-- ... other color buttons ... -->

            </LinearLayout>
        </HorizontalScrollView>

        <!-- Button to open the DatePickerDialog for setting a deadline. -->
        <Button
            android:id="@+id/setDeadlineButton"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginTop="24dp"
            android:text="Set Deadline"
            app:layout_constraintEnd_toEndOf="@id/stickyNoteCard"
            app:layout_constraintStart_toStartOf="@id/stickyNoteCard"
            app:layout_constraintTop_toBottomOf="@id/colorSelectorScrollView" />

        <!--
            This LinearLayout holds the action buttons (Discard, Delete, Save) at the bottom.
            - It's constrained to the bottom of the parent layout.
            - 'app:layout_constraintVertical_bias="1.0"' pushes it to the very bottom.
        -->
        <LinearLayout
            android:id="@+id/bottomActionLayout"
            android:layout_width="0dp"
            android:layout_height="wrap_content"
            android:layout_marginStart="24dp"
            android:layout_marginEnd="24dp"
            android:layout_marginBottom="24dp"
            android:orientation="horizontal"
            app:layout_constraintBottom_toBottomOf="parent"
            app:layout_constraintEnd_toEndOf="parent"
            app:layout_constraintStart_toStartOf="parent"
            app:layout_constraintTop_toBottomOf="@id/setDeadlineButton"
            app:layout_constraintVertical_bias="1.0">

            <!-- Discard button uses a borderless style. -->
            <Button
                android:id="@+id/discardButton"
                style="?android:attr/buttonBarButtonStyle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:layout_weight="1"
                android:text="Discard" />

            <!-- Delete button is hidden by default and only shown in Edit Mode. -->
            <Button
                android:id="@+id/deleteButton"
                style="?android:attr/buttonBarButtonStyle"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginEnd="8dp"
                android:layout_weight="1"
                android:text="Delete"
                android:textColor="#D32F2F"
                android:visibility="gone"
                tools:visibility="visible" />

            <!-- The primary action button (Save/Update). -->
            <Button
                android:id="@+id/saveButton"
                android:layout_width="0dp"
                android:layout_height="wrap_content"
                android:layout_marginStart="8dp"
                android:layout_weight="1"
                android:backgroundTint="@color/fab"
                android:text="Save"
                android:textColor="@color/white" />
        </LinearLayout>

    </androidx.constraintlayout.widget.ConstraintLayout>
</ScrollView>
